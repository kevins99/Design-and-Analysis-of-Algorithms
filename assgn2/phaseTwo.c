#include <stdio.h>
#include <string.h>

FILE *ip, *lit, *sym;
int LC = 0;
int PTP = 1;
int RC = 0;
int POOLTAB[10];
POOLTAB[0] = 1;


int main() {
    //READ FILE
    ip = fopen("output.txt", "r+");
    char line_input[40];
    while(fgets(line_input, sizeof (line_input), ip)) {
        
    }
    return 0;
    
    if(str == "LTORG") {
        
    }
}